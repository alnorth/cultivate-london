<nav>
  <span class="page-title">
    Week <%= @week_number %>
  </span>
  <span class="search">
    <button class="btn primary">Print Task List</button>
    <span>Include:</span>
    <label><input type="checkbox" id="overdue" /> Overdue tasks</label>
    <label><input type="checkbox" id="completed" /> Completed tasks</label>
  </span>
</nav>
<%= render 'reports/report_section', :title => "Sow/Cuttings", :batches => @batches.where("stage = ?", :sow) %>
<%= render 'reports/report_section', :title => "Germinate/Root", :batches => @batches.where("stage = ?", :germinate) %>
<%= render 'reports/report_section', :title => "Pot/Ground", :batches => @batches.where("stage = ?", :pot) %>
<%= render 'reports/report_section', :title => "Sale", :batches => @batches.where("stage = ?", :sale) %>
